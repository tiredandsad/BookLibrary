//need to get info by title or author. The API has a query feature
//searching can also be done by the author name or title instead of both at once.
//Use OpenLibrary for this info  https://openlibrary.org/dev/docs/api/search
//should be able to use the same for book covers and art.
//after data I think we need to show the user a list of titles related to their search
//selecting the title brings up a book card and the user can select to add it to their library


export function searchBookByQuery(query) {
    //
}

export function searchBookByTitle(title) {

}

export function searchBookByAuthor(author) {
    
}